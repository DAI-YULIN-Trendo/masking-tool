# PDF Secure Masker 安全性审查报告

本报告针对当前 PDF 遮罩处理逻辑及其物理删除机制进行了深度安全审计。

## 🔍 审计摘要
当前的实现通过物理删除 PDF 内容流（Content Stream）中的文本、图片和嵌套矢量数据（Form XObjects），并在最后通过 `prune_objects()` 清理孤立对象，安全性显著优于仅在视觉上覆盖黑框的工具。然而，由于 PDF 格式的冗余特性，仍存在几处潜在的情报泄露路径。

---

## 🚩 识别出的风险项 (Risk Items)

### 1. PDF 注释信息 (Annotations)
*   **状态**: 🔴 **未处理 (High Risk)**
*   **风险描述**: PDF 中的批注、高亮、文本框或盖章储存在 `/Annots` 字典中。
*   **影响**: 即使正文被删除，相关的批注内容（如侧边的修改意见或标注）依然保留在文件中，且可能包含敏感讨论。

### 2. 文档元数据 (Metadata)
*   **状态**: 🟡 **未处理 (Medium Risk)**
*   **风险描述**: 文档的作者、公司信息、关键词、创建日期等储存在 `/Info` 字典和 XMP 数据流中。
*   **影响**: 可能泄露文档的内部编号、编辑者身份或原始分类信息。

### 3. 页面缩略图 (Thumbnails)
*   **状态**: 🟡 **未处理 (Medium Risk)**
*   **风险描述**: PDF 可以为每一页存储预渲染的低分辨率图片用于侧边栏预览。
*   **影响**: 在 PDF 阅读器的导航栏中，用户可能仍然能看到被遮罩区域的微缩形状甚至部分内容文字。

### 4. 交互式表单字段 (Interactive Forms / AcroForm)
*   **状态**: 🔴 **未处理 (High Risk)**
*   **风险描述**: 表单的输入值存储在 `/AcroForm` 的结构中，独立于页面内容流。
*   **影响**: 虽然页面上看起来被遮住了，但通过表单工具或直接解析二进制数据，依然可以提取出表单字段的逻辑文本值。

### 5. 文档大纲/书签 (Outlines / Bookmarks)
*   **状态**: 🔵 **未处理 (Low Risk)**
*   **风险描述**: 文档左侧的导航目录。
*   **影响**: 如果章节标题本身包含敏感信息，这些文本将完整保留。

---

## 🛠️ 待处理任务清单 (To-be-Handled)

- [ ] **增强 `/Annots` 过滤**: 遍历页面注释，根据 Mask 区域自动删除或脱敏重叠的注释。
- [ ] **元数据擦除**: 在保存 PDF 前，清空 `/Info` 字典并清空 XMP Metadata 流。
- [ ] **缩略图清理**: 遍历页面字典，移除 `/Thumb` 引用，强制阅读器重新渲染（或直接保留空白预览）。
- [ ] **表单脱敏**: 识别 `/AcroForm` 中的字段，并检查其 Widget 的位置，对掩码区域内的表单值进行物理置空。
- [ ] **文档大纲审查**: 增加对 `/Outlines` 的遍历和清理逻辑。

---

## 📈 结论
目前该工具对于**物理删除正文内容**已达到企业级标准，能够有效防止通过“选择后复制”或“底层路径解析”进行的恢复尝试。但在解决上述“非结构化内容”泄露风险之前，应在显著位置提醒用户注意文档元数据及批注的安全。

---
*Created by Antigravity AI - Security Audit Module*
