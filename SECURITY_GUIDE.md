# PDF Secure Masker - 数据安全与脱敏指南

## 🛡️ 背景
本项目旨在通过物理删除 PDF 底层内容流的方式，提供比传统“覆盖黑框”更高级别的安全性。但 PDF 格式的复杂性意味着某些边角数据可能规避常规检测。

---

## 🔍 安全审计结果总结

基于对核心脱敏引擎 (`masker.rs`) 的审计，我们识别出以下关键发现：

### 已覆盖的风险点 (Safe)
- ✅ **物理正文删除**: 掩码区域内的文本 (`Tj/TJ`) 字节已被彻底清空，无法通过选择工具或解析器还原。
- ✅ **位图深度脱敏**: 对掩码下的图片 (`DCT/Flate`) 进行像素级物理重写。
- ✅ **嵌套矢量支持**: 支持 `Form XObject` 递归解析，删除深层嵌套的路径数据。
- ✅ **孤立对象清理**: 自动执行 `prune_objects()`，确保被替换的原始数据不残留在二进制文件中。

### 潜在风险与待处理项 (Action Required)
以下内容目前不在此工具的自动处理范围内，处理包含此类内容的文档时需格外小心：

| 内容类型 | 风险等级 | 说明 |
| :--- | :--- | :--- |
| **PDF 注释 (Annotations)** | 🔴 高 | 侧边栏评论、高亮和批注不属于内容流，可能泄露敏感讨论。 |
| **交互式表单 (AcroForms)** | 🔴 高 | 表单字段的“逻辑值”独立存储，遮盖外观无法删除底层值。 |
| **文档元数据 (Metadata)** | 🟡 中 | 包含作者、标题、公司名称等 XMP 数据。 |
| **页面缩略图 (Thumbnails)** | 🟡 中 | 预渲染的预览图可能显示被遮挡内容的微型文字。 |

---

## 📄 待处理文档分类建议

为了确保最高安全性，建议根据文档类型采取以下措施：

### 1. 推荐处理的文档
- 标准报告、合同正本（无批注）。
- 包含敏感照片或扫描件的文档。
- 纯文本导出的 PDF 资料。

### 2. **高风险**、需额外注意的文档
- **带有大量协作评论的草案**: 请先在 Word/编辑器中“接受所有更改”并删除批注，再导出 PDF。
- **可填写的电子表单**: 处理前建议先将表单“扁平化”（Flatten）为普通 PDF。
- **包含敏感 OLE 对象（如嵌入的 Excel）的 PDF**: 该工具目前无法脱敏嵌入的非 PDF 二进制文件。

---

## 🚀 后续改进计划
自动化解决上述风险点的路线图：
1. **v0.2.0**: 实现对 `/Annots` 坐标匹配的物理删除。
2. **v0.2.5**: 文档全局元数据（Metadata）自动擦除。
3. **v0.3.0**: 自动移除所有页面的 `/Thumb` 预览图。

---
*TRENDO 安全小组 2026.02*
